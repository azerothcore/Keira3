version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    groups:
      dev-dependencies:
        dependency-type: development
    ignore:
      # patched via patch-package, bump by hand (see patches/README.md)
      - dependency-name: ngx-bootstrap
