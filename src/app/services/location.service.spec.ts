import { TestBed } from '@angular/core/testing';

import { LocationService } from './location.service';

describe('LocationService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  // it('reload() should correctly work', () => {
  //   const service: LocationService = TestBed.get(LocationService);
  //   const handler = { locationReload: location.reload };
  //   const locationReloadSpy = spyOn(handler, 'locationReload');

  //   expect(handler.locationReload).not.toHaveBeenCalled();
  //   service.reload();
  //   expect(handler.locationReload).toHaveBeenCalled();
  // });
});
